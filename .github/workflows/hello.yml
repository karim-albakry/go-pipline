name: CI

on: [push]

jobs:
  build:
    
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, World!
    - name: run a multiline script
      run:
        echo run another action to bild
        echo test, and deploy your project
